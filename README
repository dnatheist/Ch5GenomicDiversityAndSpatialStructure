This project use 'related' which requires 32 bit version of r to be set in global options before working.

Unfortunately it also needs 64 bit R for other parts (adegenet) so needs some manual love switching between sections.

